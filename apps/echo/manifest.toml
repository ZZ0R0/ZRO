[app]
slug = "echo"
name = "Echo Test"
version = "0.1.0"
description = "Application de test complète — vérifie HTTP, WebSocket, sessions, données"

[backend]
executable = "zro-app-echo"
transport = "unix_socket"

[frontend]
directory = "frontend"
index = "index.html"

[permissions]
roles = ["admin", "user"]

[permissions.capabilities]
